# Difference from upstream

`deytt./connect` is a separate Android application. It uses the generated
`libbox` API as a networking engine and owns the DEYTTT subscription contract,
profile validation, Android VPN lifecycle, storage, and UI.

It does not claim to be the official sing-box or SFA application, and it does
not change the upstream engine in this repository.

